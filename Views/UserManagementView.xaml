<UserControl x:Class="SupermarketPOS.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:SupermarketPOS.Converters"
             Background="{StaticResource BrushBgDark}">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolVis"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <converters:BoolToStatusTextConverter x:Key="BoolToStatusTextConverter"/>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Chap tomon: Form -->
        <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0 0 16 0">
            <StackPanel>
                <TextBlock Text="Xodim profili" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushAccent}" Margin="0 0 0 16"/>

                <Label Content="ISM-SHARIF"/>
                <TextBox Style="{StaticResource TxtBox}" Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 12"/>

                <Label Content="LOGIN (Username)"/>
                <TextBox Style="{StaticResource TxtBox}" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 12"/>

                <Label Content="PAROL"/>
                <TextBox Style="{StaticResource TxtBox}" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 12"
                         ToolTip="O'zgartirish uchun yangisini yozing. Yangi xodim uchun bo'sh qolsa '12345' bo'ladi."/>

                <Label Content="LAVOZIM (Role)"/>
                <ComboBox ItemsSource="{Binding AvailableRoles}" SelectedItem="{Binding Role}" Margin="0 0 0 16"/>

                <UniformGrid Columns="2" Margin="0 0 0 8">
                    <Button Content="Tozalash" Style="{StaticResource BtnBase}" Command="{Binding ClearCommand}" Margin="0 0 4 0"/>
                    <Button Content="Saqlash" Style="{StaticResource BtnAccent}" Command="{Binding SaveCommand}" Margin="4 0 0 0"/>
                </UniformGrid>

                <Button Content="Tizimdan chetlatish (Bloklash/Ochish)" Style="{StaticResource BtnBase}" 
                        Foreground="{StaticResource BrushRed}" Command="{Binding ToggleStatusCommand}"
                        Visibility="{Binding IsEditing, Converter={StaticResource BoolVis}}"
                        Margin="0 16 0 0"/>
            </StackPanel>
        </Border>

        <!-- O'ng tomon: DataGrid -->
        <Border Grid.Column="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Mavjud xodimlar" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0 0 0 16"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="None"
                          BorderThickness="0" Background="Transparent" RowBackground="Transparent"
                          Foreground="{StaticResource BrushTextPrimary}">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource BrushBgMid}"/>
                            <Setter Property="Foreground" Value="{StaticResource BrushTextSec}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BrushBgHover}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ism-sharif" Binding="{Binding FullName}" Width="*"/>
                        <DataGridTextColumn Header="Login" Binding="{Binding Username}" Width="120"/>
                        <DataGridTextColumn Header="Lavozim" Binding="{Binding Role}" Width="100"/>
                        <DataGridTemplateColumn Header="Holati" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="6 2" HorizontalAlignment="Left" VerticalAlignment="Center"
                                            Background="{Binding IsActive, Converter={StaticResource BoolToColorConverter}}">
                                        <TextBlock Text="{Binding IsActive, Converter={StaticResource BoolToStatusTextConverter}}" 
                                                   Foreground="White" FontSize="11" FontWeight="Bold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Sana" Binding="{Binding CreatedAt, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
